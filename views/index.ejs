<%- include('./partials/header') %>

<div class="w-full min-h-screen flex items-center justify-center px-4 py-10">

    <div class="w-full max-w-md bg-white shadow-2xl rounded-2xl p-8">

        <h2 class="text-3xl font-bold text-center mb-2">
            Login to <span class="text-blue-500">PACKORA</span>
        </h2>

        <p class="text-gray-500 text-center mb-6">
            Welcome back
        </p>

        <form action="/users/login" method="POST" class="space-y-4">

            <input
                type="email"
                name="email"
                placeholder="Email"
                class="w-full bg-zinc-100 px-4 py-3 rounded-lg border"
                required
            >

            <input
                type="password"
                name="password"
                placeholder="Password"
                class="w-full bg-zinc-100 px-4 py-3 rounded-lg border"
                required
            >

            <button class="w-full bg-blue-500 hover:bg-blue-600 text-white py-3 rounded-lg">
                Login
            </button>

        </form>

        <p class="text-center mt-6 text-sm">
            Don’t have an account?
            <a href="/users/signup" class="text-blue-500 font-semibold hover:underline">
                Sign Up
            </a>
        </p>

    </div>

</div>

<%- include('./partials/footer') %>
